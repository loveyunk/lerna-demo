{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "yarn workspace @loveyunk/lerna-app1 test",
    "start": "lerna run start --since",
    "new-version": "lerna version --yes",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish"
  }
}
